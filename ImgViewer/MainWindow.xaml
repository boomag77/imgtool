<Window x:Class="ImgViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImgViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menu bar row -->
            <RowDefinition Height="*"/>
            <!-- Main content row -->
            <RowDefinition Height="Auto"/>
            <!--Status bar row -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <!--Folder images list -->
            <ColumnDefinition Width="6*"/>
            <!--Image preview -->
            <ColumnDefinition Width="3*"/>
            <!--Controls -->
        </Grid.ColumnDefinitions>

        <!--Menu -->
        <Menu Grid.Row="0" VerticalAlignment="Top" Grid.ColumnSpan="3">
            <MenuItem Header="_File">
                <MenuItem Header="_Open File" Click="OpenFile"/>
                <Separator/>
                <MenuItem Header="_Open Folder" Click="OpenFolder"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="ExitClick"/>
            </MenuItem>
        </Menu>
        <StatusBar Grid.Row="2" Grid.ColumnSpan="3" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready" />
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar x:Name="MyProgressBar" Width="200" Height="16" Minimum="0" Maximum="100" />
            </StatusBarItem>
        </StatusBar>
            
            <!--Folder images list -->
        <ListBox x:Name="ImgListBox"
                 Grid.Row="1" Grid.Column="0"
                 Margin="5"
                 SelectionChanged="ImgList_SelectionChanged"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.IsDeferredScrollingEnabled="False"
                 ScrollViewer.CanContentScroll="False">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <Image Source="{Binding Thumb}"
                               Width="80" Height="80" Stretch="UniformToFill"
                               Margin="5"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                    </StackPanel>
                
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--Image preview -->
        <Border Grid.Row="1" Grid.Column="1"
                BorderThickness="1"
                BorderBrush="#FFBDBDBD"
                Margin="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                SnapsToDevicePixels="True">
            <Viewbox Stretch="Uniform">
                <Image x:Name="ImgBox"
                Stretch="None"/>
            </Viewbox>
                    
            
                
        </Border>
        <!--Controls -->
        <StackPanel Grid.Row="1" Grid.Column="2" Margin="5">
            <TextBlock Text="Controls" FontWeight="Bold" FontSize="16" Margin="5"/>
            <Separator Margin="5"/>
            <Button Content="Open" Click="OpenFile" Margin="5"/>
            <Button Content="Apply Deskew" Click="ApplyDeskew" Margin="5"/>
            <Button Content="Remove borders" Click="ApplyAutoCropRectangleCurrentCommand" Margin="5"/>
            <Button Content="Auto crop" Click="ApplyAutoCropRectangleCurrentCommand" Margin="5"/>
            <Button Content="Run Despeckle" Click="ApplyDespeckleCommand" Margin="5"/>
            <Button Content="AutoBinarize" Click="ApplyAutoBinarizeCommand" Margin="5"/>
            <Button Content="Lines remove" Click="ApplyLineRemoveCommand" Margin="5"/>
            <Button Content="Remove Punches" Click="ApplyPunchesRemoveCommand" Margin="5"/>
            <Button Content="Process Folder" Click="ProcessFolderClick" Margin="5"/>
            <Separator Margin="10"/>
            <Button Content="Save As..." Click="SaveAsClick" Margin="5"/>
        </StackPanel>

    </Grid>
    
</Window>
