<Window x:Class="ImgViewer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImgViewer"
        xmlns:views="clr-namespace:ImgViewer.Views"
        xmlns:conv="clr-namespace:ImgViewer.Converters"
        mc:Ignorable="d"
        Title="CCS Image Tool" Height="600" Width="800"
        Background="LightGray">
    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InvBoolToVis"/>

        <SolidColorBrush x:Key="PipelineCardBackgroundBrush" Color="#2F3136"/>
        <SolidColorBrush x:Key="PipelineCardBorderBrush" Color="#464A52"/>
        <SolidColorBrush x:Key="PipelineTextBrush" Color="#E5E7EA"/>
        <SolidColorBrush x:Key="PipelineTextboxBackgroundBrush" Color="#1E2024"/>
        <Style x:Key="SpinnerRepeatButtonStyle" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PipelineCardListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2F3136"/>
            <Setter Property="BorderBrush" Value="#2F3136"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1E66E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1553C0"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ElevatedRoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#2F3136"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- shadow -->
                            <Border CornerRadius="6" Background="Transparent" Margin="0,2,0,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                                </Border.Effect>
                            </Border>

                            <!-- main -->
                            <Border x:Name="bd"
                            CornerRadius="4"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Pipeline-styled ComboBox (paste inside Window.Resources) -->
        <Style x:Key="PipelineComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource PipelineTextBrush}" />
            <Setter Property="Background" Value="{StaticResource PipelineTextboxBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PipelineCardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="MinWidth" Value="50" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- content goes in column 0 and fills available space -->
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Right"   
                                                                        Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      RecognizesAccessKey="True" /> 

                                    <!-- toggle sits in column 1 with fixed width and won't stretch -->
                                        <ToggleButton x:Name="Toggle"
                                                      Grid.Column="1"
                                                      Width="20"
                                                      Focusable="False"
                                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                      Background="Transparent"
                                                      BorderBrush="Transparent"
                                                      BorderThickness="0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Right"
                                                      Margin="4,0,0,0">
                                            <!-- small outer padding -->
                                            <Viewbox Width="10" Height="10">
                                                <Path Data="M0,0 L4,4 8,0 Z" Fill="{StaticResource PipelineTextBrush}"/>
                                            </Viewbox>
                                        </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource PipelineCardBackgroundBrush}"
                                        BorderBrush="{StaticResource PipelineCardBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,6,0,0"
                                        SnapsToDevicePixels="True">
                                    <ScrollViewer CanContentScroll="True"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto"
                                                  MaxHeight="280"
                                                  Padding="2">  
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem style (list items in popup) -->
        <Style x:Key="PipelineComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource PipelineTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PipelineCardBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1E66E0"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Put this inside <Window.Resources> (replace any previous PipelineCheckBoxStyle) -->
        <Style x:Key="PipelineCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource PipelineTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <!-- Box -->
                            <Border x:Name="Box"
                            Width="18" Height="18"
                            CornerRadius="3"
                            Background="{StaticResource PipelineTextboxBackgroundBrush}"
                            BorderBrush="{StaticResource PipelineCardBorderBrush}"
                            BorderThickness="1"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"/>

                            <!-- Vector check mark - stroke binds to Foreground so it matches text -->
                            <Path x:Name="CheckMark"
                          Data="M3,9 L7,13 L15,4"
                          Stroke="{TemplateBinding Foreground}"
                          StrokeThickness="2.0"
                          StrokeLineJoin="Round"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          Visibility="Collapsed"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center"
                          Margin="0,0,0,0"/>

                            <!-- Label: use TextElement.Foreground to color text correctly -->
                            <ContentPresenter x:Name="content"
                                      ContentSource="Content"
                                      Margin="24,0,0,0"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"
                                      TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- show check when checked -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <!-- optional: change box background on checked -->
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource PipelineCardBorderBrush}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>




    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <!-- Menu bar row -->
            <RowDefinition Height="Auto"/>
            <!--Upper controls -->
            <RowDefinition Height="Auto"/>
            <!-- Main content row -->
            <RowDefinition Height="*"/>
            <!--Lower controls-->
            <RowDefinition Height="Auto"/>
            <!--Status bar row -->
            <RowDefinition Height="Auto"/>
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <!--Original image -->
            <ColumnDefinition Width="2*"/>
            <!--Pipeline -->
            <ColumnDefinition Width="4*"/>
            <!--Processing image -->
        </Grid.ColumnDefinitions>

        <!--Menu -->
        <Menu Grid.Row="0" VerticalAlignment="Top" Grid.ColumnSpan="3">
            <MenuItem Header="_File">
                <MenuItem Header="_Open File" Click="OpenFile_Click"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="ExitClick"/>
            </MenuItem>
        </Menu>
        
        <!-- Status bar -->
        <StatusBar Grid.Row="4" Grid.ColumnSpan="3" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="{Binding Status}" />
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar x:Name="MyProgressBar" Width="200" Height="16" Minimum="0" Maximum="100" />
            </StatusBarItem>
        </StatusBar>
            
        
        <!--Original Image-->
        <Border Grid.Row="2" Grid.Column="0"
        BorderThickness="1"
        BorderBrush="#FFBDBDBD"
        Margin="5"
        Padding="5"
        
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        SnapsToDevicePixels="True">
            <Viewbox Stretch="Uniform">
                <Image x:Name="OrigImgBox"
               RenderOptions.BitmapScalingMode="HighQuality"
               Source="{Binding OriginalImage}"
        Stretch="None"/>
            </Viewbox>
        </Border>

        <Border Grid.Row="1" Grid.Column="2">
            <Button Style="{StaticResource ElevatedRoundedButton}"
                 Margin="5,5,5,5"
                    Visibility="{Binding IsProcessingImageOnPreview}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                 MinWidth="120"
                 Click="ResetButton_Click">Reset Image</Button>
               
        </Border>

        <Border Grid.Row="1" Grid.Column="0">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Button Style="{StaticResource ElevatedRoundedButton}"
                        Margin="5,5,5,5"
                        MinWidth="120"
                        Click="OpenFile_Click">Open Image</Button>
                <Button Style="{StaticResource ElevatedRoundedButton}"
                        Margin="5,5,5,5"
                        MinWidth="120"
                        Click="SaveAs_Click">Save Image As...</Button>
                
            </StackPanel>
             
        </Border>

        <Border Grid.Row="3" Grid.Column="0">
            <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Button Style="{StaticResource ElevatedRoundedButton}"
                Margin="5,5,5,5"
                MinWidth="120"
                Click="OpenPrevFile_Click">Prev</Button>
                <Button Style="{StaticResource ElevatedRoundedButton}"
                Margin="5,5,5,5"
                MinWidth="120"
                Click="OpenNextFile_Click">Next</Button>

            </StackPanel>

        </Border>

        <!--Pipeline -->
        <ListBox x:Name="PipelineListBox"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="0"
                 Padding="5,0,0,5"
                 ItemsSource="{Binding PipeLineOperations, RelativeSource={RelativeSource AncestorType=Window}}"
                 Background="Transparent"
                 BorderThickness="0"
                 AllowDrop="True"
                 HorizontalContentAlignment="Stretch"
                 ItemContainerStyle="{StaticResource PipelineCardListBoxItemStyle}"
                 PreviewMouseLeftButtonDown="PipelineListBox_PreviewMouseLeftButtonDown"
                 PreviewMouseMove="PipelineListBox_PreviewMouseMove"
                 DragEnter="PipelineListBox_DragEnter"
                 DragOver="PipelineListBox_DragOver"
                 DragLeave="PipelineListBox_DragLeave"
                 Drop="PipelineListBox_Drop"
                 GiveFeedback="PipelineListBox_GiveFeedback">
            <ListBox.Resources>
                <Style TargetType="ScrollViewer">
                    <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                    <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type views:PipeLineOperation}">
                    <Border Background="{StaticResource PipelineCardBackgroundBrush}"
                            BorderBrush="{StaticResource PipelineCardBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            SnapsToDevicePixels="True">
                        <StackPanel>
                            <!-- header: title left, "In pipeline" checkbox right -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding DisplayName}"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Foreground="{StaticResource PipelineTextBrush}"
                                           VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0" >
                                    <CheckBox Content="In pipeline"
                                              IsChecked="{Binding InPipeline, Mode=TwoWay}"
                                              Style="{StaticResource PipelineCheckBoxStyle}"
                                              VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <CheckBox Content="Live"
                                              IsChecked="{Binding Live, Mode=TwoWay}"
                                              Style="{StaticResource PipelineCheckBoxStyle}"
                                              VerticalAlignment="Center" /> 
                                </StackPanel>

                            </Grid>
                            <ItemsControl ItemsSource="{Binding Parameters}">
                                <ItemsControl.Style>
                                    <Style TargetType="ItemsControl">
                                        <Setter Property="Margin" Value="0,0,0,4"/>
                                        <Style.Triggers>
                                            <Trigger Property="HasItems" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type views:PipeLineParameter}">
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0,0,12,8"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding IsVisible, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="{Binding Label}"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource PipelineTextBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,6,0"/>               

                                            <!-- Outer border kept same -->
                                            <Border Background="{StaticResource PipelineTextboxBackgroundBrush}"
                                                    BorderBrush="{StaticResource PipelineCardBorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="3">   
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="18"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- ComboBox: visible when IsCombo == true -->
                                                    <ComboBox Grid.Column="0"
                                                              MinWidth="50"
                                                              MaxWidth="140"
                                                              ItemsSource="{Binding Options}"
                                                              SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                                                              Visibility="{Binding IsCombo, Converter={StaticResource BoolToVis}}"
                                                              Style="{StaticResource PipelineComboBoxStyle}"
                                                              ItemContainerStyle="{StaticResource PipelineComboBoxItemStyle}"
                                                              Background="Transparent"
                                                              Foreground="{StaticResource PipelineTextBrush}"
                                                              BorderThickness="0"
                                                              HorizontalContentAlignment="Right"
                                                              VerticalContentAlignment="Center"
                                                              FontSize="13"/>   


                                                    <!-- Numeric area: visible when IsCombo == false -->
                                                    <Grid Grid.Column="0" Visibility="{Binding IsCombo, Converter={StaticResource InvBoolToVis}}">
                                                        <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.##}}"
                                                                 Background="Transparent"
                                                                 Foreground="{StaticResource PipelineTextBrush}"
                                                                 BorderThickness="0"
                                                                 Padding="6,2"
                                                                 HorizontalContentAlignment="Right"
                                                                 VerticalContentAlignment="Center"
                                                                 MinWidth="50"
                                                                 MaxWidth="140"
                                                                 FontSize="13"/>    
                                                    </Grid>

                                                    <!-- Spinners: keep them in Column=1 but only visible for numeric -->
                                                    <Grid Grid.Column="1" Visibility="{Binding IsCombo, Converter={StaticResource InvBoolToVis}}">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition/>
                                                            <RowDefinition/>
                                                        </Grid.RowDefinitions>

                                                        <RepeatButton Grid.Row="0"
                                                                      Click="SpinnerUp_Click"
                                                                      Style="{StaticResource SpinnerRepeatButtonStyle}">    
                                                            <Viewbox Width="8" Height="5">
                                                                <Path Data="M0,4 L4,0 8,4 Z"
                                                                      Fill="{StaticResource PipelineTextBrush}"/>
                                                            </Viewbox>
                                                        </RepeatButton>

                                                        <RepeatButton Grid.Row="1"
                                                                      Click="SpinnerDown_Click"
                                                                      Style="{StaticResource SpinnerRepeatButtonStyle}">
                                                            <Viewbox Width="8" Height="5">
                                                                <Path Data="M0,0 L4,4 8,0 Z"
                                                                      Fill="{StaticResource PipelineTextBrush}"/>
                                                            </Viewbox>
                                                        </RepeatButton>
                                                    </Grid>
                                                </Grid>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>

                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="{Binding ActionLabel}"
                                    Margin="0,8,0,0"
                                    HorizontalAlignment="Right"
                                    Padding="12,4"
                                    MinWidth="120"
                                    Click="PipelineRunButton_Click"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--Image preview -->
        <Border Grid.Row="2" Grid.Column="2"
        BorderThickness="1"
        BorderBrush="#FFBDBDBD"
        Margin="5"
        Padding="5"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        SnapsToDevicePixels="True">
            <Viewbox Stretch="Uniform">
                <Image x:Name="PreviewImgBox"
               RenderOptions.BitmapScalingMode="HighQuality"
               Source="{Binding ImageOnPreview}"
        Stretch="None"/>
            </Viewbox>
        </Border>

    </Grid>
    
</Window>
