<Window x:Class="ImgViewer.Views.BatchResizeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch resize"
        Height="430"
        Width="640"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Longest image dimension (px):" VerticalAlignment="Center"/>
            <TextBox x:Name="LongestDimensionTextBox"
                     Width="90"
                     Margin="8,0,16,0"
                     Text="1920"/>

            <TextBlock Text="Resize method:" VerticalAlignment="Center"/>
            <ComboBox x:Name="ResizeMethodComboBox"
                      Width="180"
                      Margin="8,0,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Grid.ColumnSpan="2"
                   Text="Folders to resize:"
                   Margin="0,10,0,6"/>

        <ListBox x:Name="FoldersListBox"
                 Grid.Row="2"
                 Grid.Column="0"
                 Margin="0,0,10,0"/>

        <StackPanel Grid.Row="2"
                    Grid.Column="1"
                    Orientation="Vertical">
            <Button Content="Add folder..."
                    Width="120"
                    Margin="0,0,0,6"
                    Click="AddFolder_Click"/>
            <Button Content="Remove selected"
                    Width="120"
                    Margin="0,0,0,6"
                    Click="RemoveSelectedFolder_Click"/>
            <Button Content="Clear all"
                    Width="120"
                    Click="ClearFolders_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="StartResizeButton"
                    Content="Start resizing"
                    Width="130"
                    Margin="0,0,8,0"
                    Click="StartResize_Click"/>
            <Button Content="Close"
                    Width="90"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
