<Window x:Class="ImgViewer.Views.BatchResizeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch resize"
        Height="470"
        Width="860"
        MinWidth="820"
        WindowStartupLocation="CenterOwner"
        Background="#1C1F24">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#2F3136"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#464A52"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#E5E7EA"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#AAB0B7"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#1E2024"/>
        <Style x:Key="InlineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3A3E46"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#45515F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A3441"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>

        <Style x:Key="InputComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>

        <Style x:Key="InputComboStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource InputComboItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False"
                                          ClickMode="Press"
                                          OverridesDefaultStyle="True"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Border x:Name="ComboBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="24"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0"
                                                          Margin="8,0,0,0"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                                        <Path Grid.Column="1"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="{TemplateBinding Foreground}"/>
                                    </Grid>
                                </Border>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Focusable="False"
                                   AllowsTransparency="True"
                                   PopupAnimation="Slide">
                                <Border Background="{StaticResource InputBackgroundBrush}"
                                        BorderBrush="{StaticResource PanelBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,2,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Border Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="Longest dimension (px):"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"/>
                    <TextBox x:Name="LongestDimensionTextBox"
                             Grid.Row="0"
                             Grid.Column="1"
                             Style="{StaticResource InputTextBoxStyle}"
                             Text="1920"/>

                    <TextBlock Grid.Row="0"
                               Grid.Column="3"
                               Text="Resize method:"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"/>
                    <ComboBox x:Name="ResizeMethodComboBox"
                              Grid.Row="0"
                              Grid.Column="4"
                              Margin="0,0,6,0"
                              Style="{StaticResource InputComboStyle}"/>

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="JPG quality (if applicable):"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               Margin="0,8,10,0"
                               VerticalAlignment="Center"/>
                    <TextBox x:Name="JpegQualityTextBox"
                             Grid.Row="1"
                             Grid.Column="1"
                             Style="{StaticResource InputTextBoxStyle}"
                             Margin="0,8,0,0"
                             Text="90"/>
                    <TextBlock Grid.Row="1"
                               Grid.Column="3"
                               Grid.ColumnSpan="2"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Text="Applied only when source file extension is .jpg or .jpeg."/>
                </Grid>

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Text="Folders to resize:"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           Margin="0,0,0,6"/>

                <ListBox x:Name="FoldersListBox"
                         Grid.Row="2"
                         Grid.Column="0"
                         Margin="0,0,10,0"
                         Foreground="{StaticResource PrimaryTextBrush}"
                         Background="{StaticResource InputBackgroundBrush}"
                         BorderBrush="{StaticResource PanelBorderBrush}"
                         BorderThickness="1"/>

                <StackPanel Grid.Row="2"
                            Grid.Column="1"
                            Orientation="Vertical">
                    <Button Content="Add folder..."
                            Style="{StaticResource InlineButtonStyle}"
                            Width="130"
                            Margin="0,0,0,6"
                            Click="AddFolder_Click"/>
                    <Button Content="Remove selected"
                            Style="{StaticResource InlineButtonStyle}"
                            Width="130"
                            Margin="0,0,0,6"
                            Click="RemoveSelectedFolder_Click"/>
                    <Button Content="Clear all"
                            Style="{StaticResource InlineButtonStyle}"
                            Width="130"
                            Click="ClearFolders_Click"/>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,12,0,0">
                    <Button x:Name="StartResizeButton"
                            Content="Start resizing"
                            Style="{StaticResource InlineButtonStyle}"
                            Width="130"
                            Margin="0,0,8,0"
                            Click="StartResize_Click"/>
                    <Button Content="Close"
                            Style="{StaticResource InlineButtonStyle}"
                            Width="90"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
