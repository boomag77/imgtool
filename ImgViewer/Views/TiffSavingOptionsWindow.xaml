<Window x:Class="ImgViewer.Views.TiffSavingOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImgViewer.Views"
        mc:Ignorable="d"
        Title="TIFF saving options" Height="260" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select TIFF-compression type:" FontWeight="Bold"/>

        <ComboBox Name="CompressionComboBox"
                  Grid.Row="1"
                  Margin="0,8,0,8"
                  Height="26"
                  DisplayMemberPath="Key"
                  SelectionChanged="CompressionComboBox_SelectionChanged"/>

        <Border x:Name="JpegOptionsPanel"
                Grid.Row="2"
                Margin="0,4,0,10"
                Padding="8"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="JPEG options" FontWeight="Bold" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Quality (1-100):" Margin="0,0,8,6"/>
                <TextBox x:Name="JpegQualityTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,6" VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Subsampling:" Margin="0,0,8,0"/>
                <ComboBox x:Name="SubsamplingComboBox" Grid.Row="2" Grid.Column="1" DisplayMemberPath="Key"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="OK" Width="80" Margin="0,0,8,0" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
